<?xml version="1.0" encoding="UTF-8"?>

<!-- 
    Leverages sso simulator provided login page.

    Processing supports aliases with processing instructions of form name=value
    and for each string value read thereafter replaces any occurrence of 
    {{name}} with the specified value in the alias declaration.
-->

<?alias rest-port=1776?>
<?alias console-port={{rest-port}}?>
<?alias http-port=80?>

<!-- 
    Defines the configuration for the sso simulator. The console-port is the
    port on which the console will be found and the rest api resides. The proxy
    port is the port that should be set as the browser's http proxy.
-->
<config proxy-port="{{http-port}}" console-port="{{console-port}}" allow-non-sso-traffic="false" rest-version='openSSO'>
    <console-recording sso="true" rest="true" max-entries="100" enable-debug-logging="true"/>
    <sso-cookie name="lds-policy" domain=".lds.org"/>
    <sso-sign-in-url value="http://local.lds.org:{{console-port}}/admin/codaUserSelect.jsp"/> 
    <sso-header name="policy-service-url" value="http://local.lds.org:{{rest-port}}/rest/"/>
     
    <sso-traffic>
        <by-site host="local.lds.org" port="{{http-port}}">
            
            <cctx-mapping cctx="/public/*" thost="127.0.0.1" tport="{{console-port}}" tpath="/admin/*"/>
            <unenforced cpath="/public/debug.jsp"/>
            <unenforced cpath="/public/debug.jsp?*"/>

            <cctx-mapping cctx="/secure/*" thost="127.0.0.1" tport="{{console-port}}" tpath="/admin/*"/>
            <allow action="GET,POST" cpath="/secure/debug.jsp*"/>
            <allow action="GET,POST" cpath="/secure/debug.jsp?*"/>

            <!-- <cctx-mapping cctx="/ysa/secure/*" thost="10.109.50.85" tport="8080" tpath="/ysa/secure/*"/>
            <allow action="GET,POST" cpath="/ysa/secure/*"/>
            <allow action="GET,POST" cpath="/ysa/secure/*?*"/> -->

            <cctx-mapping cctx="/ysa/app/*" thost="10.109.50.85" tport="8080" tpath="/ysa/app/*"/>
            <unenforced cpath="/ysa/app/*"/>
            <unenforced cpath="/ysa/app/*?*"/>

        </by-site>
    </sso-traffic>
    <!-- 
        The set of defined users, headers injected per user, and urls allowed 
        by each user url access permissions 
    -->
    <users source="http://tech.lds.org/coda/services/1/member/osso/{username}" session-timeout-seconds="18000">
        <user name="pholder" pwd="password1">
            <sso-header name="policy-birthdate" value="1980-03-31"/>
            <sso-header name="policy-email" value="admin@localhost"/>
            <sso-header name="policy-preferred-name" value="Perry Holder"/>
            <sso-header name="policy-sn" value="Holder"/>
            <sso-header name="policy-lds-mrn" value="0000025601091"/>
            <sso-header name="policy-lds-account-id" value="11"/>
            <sso-header name="policy-units" value="W17701:S501484:A790036"/>
            <sso-header name="policy-positions" value="P4:W17701:S501484:A790036"/>
            <sso-header name="policy-preferred-language" value="en"/>
        </user>
    </users>
</config>
