<?xml version="1.0" encoding="UTF-8"?>

<!-- 
    Example showing the simplest configuration file that can be used.

    The console-port is the
    port on which the console will be found and the rest api resides. The proxy
    port is the port that represents the unified site or can be used as the
    browser's http proxy if desired for particular test scenarios. When using 
    the auto-bind feature the resulting ports can be seen on the output or is 
    accessible via Config.getInstance().getConsolePort() and 
    Config.getInstance().getProxyPort().
-->
<config proxy-port="auto" console-port="auto" rest-version="CD-OESv1">
    <console-recording sso="true" rest="true" max-entries="1000" enable-debug-logging="true"/>
    <sso-traffic>
      <by-site host='local.lds.org' port='{{proxy-port}}'>
      </by-site>
    </sso-traffic>
</config>
