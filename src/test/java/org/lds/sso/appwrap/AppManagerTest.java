package org.lds.sso.appwrap;

import org.testng.Assert;
import org.testng.annotations.Test;

public class AppManagerTest {
/*
	@Test
	public void testUnenforced() {
		TrafficManager mgr = new TrafficManager();
		mgr.addUnenforcedUrl("/some/wild/*");
		mgr.addUnenforcedUrl("/some/specific/path");
		Assert.assertTrue(mgr.isUnenforced("/some/wild/card/path"), "path should match wild card");
		Assert.assertFalse(mgr.isUnenforced("/some/path"), "path should not match specific or wild card");
		Assert.assertTrue(mgr.isUnenforced("/some/specific/path"), "path should match specific path");
	}

	@Test
	public void testIsPermitted() {
		TrafficManager mgr = new TrafficManager();
		mgr.addPermittedUrl("GET", "/some/wild/*");
		mgr.addPermittedUrl("PUT", "/some/wild/*");
		mgr.addPermittedUrl("GET", "/some/specific/path");
		mgr.addPermittedUrl("PUT", "/some/specific/path");
		Assert.assertTrue(mgr.isPermittedForAuthdUsers("GET", "/some/wild/card/path"), "GET on path should match wild card");
		Assert.assertTrue(mgr.isPermittedForAuthdUsers("PUT", "/some/wild/card/path"), "PUT on path should match wild card");
		Assert.assertFalse(mgr.isPermittedForAuthdUsers("POST", "/some/wild/card/path"), "POST should not match");
		Assert.assertFalse(mgr.isPermittedForAuthdUsers("GET","/some/path"), "GET on path should not match specific or wild card");
		Assert.assertTrue(mgr.isPermittedForAuthdUsers("PUT","/some/specific/path"), "PUT path should match specific path");
		Assert.assertTrue(mgr.isPermittedForAuthdUsers("GET","/some/specific/path"), "GET path should match specific path");
		Assert.assertFalse(mgr.isPermittedForAuthdUsers("POST","/some/specific/path"), "POST should not match");
	}
	*/
}
